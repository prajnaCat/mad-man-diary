<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="BookDao">
    <insert id="create" parameterType="Book" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO book(serial) VALUES (#{serial})
    </insert>

    <resultMap id="BaseResultMap" type="com.mmd.book.entities.Book">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <id column="serial" property="serial" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="getBookById" parameterType="Long" resultMap="BaseResultMap">
        SELECT * FROM book WHERE id = #{id}
    </select>

</mapper>