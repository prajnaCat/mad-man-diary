<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="WriterDao">
    <insert id="create" parameterType="Writer" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO writer(serial) VALUES (#{serial})
    </insert>

    <resultMap id="BaseResultMap" type="com.mmd.writer.entities.Writer">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <id column="serial" property="serial" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="getWriterById" parameterType="Long" resultMap="BaseResultMap">
        SELECT * FROM writer WHERE id = #{id}
    </select>

</mapper>